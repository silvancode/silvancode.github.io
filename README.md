# Github Pages
1. Create a new public repository named `username.github.io`
2. Commit and push a index.html file to the repository
3. Open `username.github.io` in the browser of your choice

# Docker
1. Create file `dockerfile`
2. Build a image with `docker build -t dockerfile .`
3. Run image with **Docker Desktop**